# Server
spring.application.name=cart-service
server.port=8085

# Redis
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# Cart TTL — reset after user activity
cart.ttl.days=7
